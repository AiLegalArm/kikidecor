

## Plan: Enlarge and center footer logo

**File:** `src/components/Layout.tsx`, line 190

**Changes:**
- Increase logo height from `h-12` to `h-20`
- Center the logo by adding `mx-auto` class
- Optionally add `block` to ensure centering works

